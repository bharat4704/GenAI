import datetime

class Player:
    def __init__(self, fname, lname, birth_year):
        self.first_name = fname
        self.last_name = lname
        self.birth_year = birth_year

    def get_age(self):
        now = datetime.datetime.now()
        return now.year - self.birth_year


class TennisPlayer(Player):
    def __init__(self, fname, lname, birth_year):
        super().__init__(fname, lname, birth_year)
        self.aces = []

    def add_aces(self, aces):
        self.aces.append(aces)

    def get_average_aces_per_match(self):
        return sum(self.aces) / len(self.aces) if self.aces else 0



class CricketPlayer(Player):
    def __init__(self, fname, lname, team, birth_year):
        super().__init__(fname, lname, birth_year)
        self.team = team
        self.scores = []

    def add_score(self, score):
        self.scores.append(score)

    def get_average_score(self):
        return sum(self.scores) / len(self.scores) if self.scores else 0



tennis_players = [
    TennisPlayer("Roger", "Federer", 1981),
    TennisPlayer("Rafael", "Nadal", 1986),
    TennisPlayer("Novak", "Djokovic", 1987),
    TennisPlayer("Andy", "Murray", 1987),
    TennisPlayer("Carlos", "Alcaraz", 2003),
    TennisPlayer("Daniil", "Medvedev", 1996),
    TennisPlayer("Stefanos", "Tsitsipas", 1998),
    TennisPlayer("Alexander", "Zverev", 1997),
    TennisPlayer("Dominic", "Thiem", 1993),
    TennisPlayer("Stan", "Wawrinka", 1985),
    TennisPlayer("Nick", "Kyrgios", 1995),
    TennisPlayer("Casper", "Ruud", 1998),
    TennisPlayer("Jannik", "Sinner", 2001),
    TennisPlayer("Taylor", "Fritz", 1997),
    TennisPlayer("Felix", "Auger-Aliassime", 2000)
]


for player in tennis_players:
    player.add_aces(8)
    player.add_aces(10)
    player.add_aces(12)



cricket_players = [
    CricketPlayer("Virat", "Kohli", "India", 1988),
    CricketPlayer("Rohit", "Sharma", "India", 1987),
    CricketPlayer("KL", "Rahul", "India", 1992),
    CricketPlayer("Shubman", "Gill", "India", 1999),
    CricketPlayer("Hardik", "Pandya", "India", 1993),
    CricketPlayer("David", "Warner", "Australia", 1988),
    CricketPlayer("Steve", "Smith", "Australia", 1989),
    CricketPlayer("Joe", "Root", "England", 1990),
    CricketPlayer("Ben", "Stokes", "England", 1991),
    CricketPlayer("Babar", "Azam", "Pakistan", 1994),
    CricketPlayer("Kane", "Williamson", "New Zealand", 1990),
    CricketPlayer("AB", "de Villiers", "South Africa", 1984),
    CricketPlayer("Quinton", "de Kock", "South Africa", 1992),
    CricketPlayer("Shakib", "Al Hasan", "Bangladesh", 1987),
    CricketPlayer("Jos", "Buttler", "England", 1990)
]


for player in cricket_players:
    player.add_score(35)
    player.add_score(70)
    player.add_score(90)



print("\nTENNIS PLAYERS")
print("-" * 40)
for player in tennis_players:
    print(
        f"{player.first_name} {player.last_name} | "
        f"Age: {player.get_age()} | "
        f"Avg Aces: {player.get_average_aces_per_match():.2f}"
    )



print("\nCRICKET PLAYERS")
print("-" * 40)
for player in cricket_players:
    print(
        f"{player.first_name} {player.last_name} | "
        f"Team: {player.team} | "
        f"Age: {player.get_age()} | "
        f"Avg Score: {player.get_average_score():.2f}"
    )
